{
  "buildCommand": "pnpm install && pnpm build:cli && pnpm build:site",
  "outputDirectory": "dist-site",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
